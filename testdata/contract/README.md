# Contract fixtures

Golden-style response shapes adapted from DatoriumDB
`test/contract/golden/` for client-side decoding tests.

When updating, note the source file and server git revision in the commit
message so protocol drift stays reviewable.
